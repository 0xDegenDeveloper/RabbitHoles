[package]
name = "rabbitholes"
version = "0.1.0"

# See more keys and their definitions at https://docs.swmansion.com/scarb/docs/reference/manifest

[dependencies]
# rbits = { path = "rbits" }
# manager = { path = "manager" }
# archive = { path = "archive" }

[scripts]
test = "cairo-test --starknet tests/lib.cairo"

